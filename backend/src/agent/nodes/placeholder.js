// Placeholder for LangGraph agent nodes.
// Each node will represent a step in the research process.

export const dummyNode = async (state) => {
  console.log("Executing dummy node");
  return state;
};
